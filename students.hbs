<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/js/delete_student.js"></script>
    <link rel="stylesheet" href="css/style.css">
    <title>Students</title>
</head>
<body>
    <h2>Students</h2>
    <nav>
        <a href="/">Home</a>
        <a href="/students">Students</a>
        <a href="/instructors">Instructors</a>
        <a href="/classes">Classes</a>
        <a href="/clubs_eas">Clubs</a>
        <a href="/students_in_classes">Students in Classes</a>
        <a href="/students_in_clubs">Students in Clubs</a>
    </nav>
    
    <table id="students-table">

        {{!-- Header section --}}
        <thead>

            {{!-- For just the first row, we print each key of the row object as a header cell so we
            know what each column means when the page renders --}}
            <tr>
                {{#each data.[0]}}
                <th>
                    {{@key}}
                </th>
                {{/each}}
            </tr>
        </thead>

        {{!-- Body section --}}
        <tbody>

            
            {{#each data}}
            <tr>
                <td>{{this.student_id}}</td>
                <td>{{this.first_name}}</td>
                <td>{{this.last_name}}</td>
                <td>{{this.grade_level}}</td>
                <td>{{this.gpa}}</td>
                <td class="actions">
                    <a href="/update_student?student_id={{this.student_id}}" class="update">Edit</a>
                    <a href="#" class="delete" data-student-id="{{this.student_id}}" onclick="deleteStudent(event, '{{this.student_id}}')">Delete</a>
                    <form action="/delete_student" method="POST" id="deleteForm">
                        <input type="hidden" name="student_id" id="deleteStudentId">
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <a href="/add_student">Add New Student</a>
</body>
</html>