<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Student in Class</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <nav>
        <a href="/students_in_classes">Back to Students in Classes</a>
    </nav>
    <h1>Update Student in Class</h1>
    <form action="/update_student_in_class" method="POST">
        <input type="hidden" name="students_in_classes_id" value="{{studentInClass.students_in_classes_id}}">
        
        <label for="student-id">Student:</label>
        <select id="student-id" name="student_id" required>
            {{#each students}}
            <option value="{{this.student_id}}" {{#if (eq this.student_id ../studentInClass.student_id)}}selected{{/if}}>
                {{this.first_name}} {{this.last_name}}
            </option>
            {{/each}}
        </select>
        
        <label for="class-id">Class:</label>
        <select id="class-id" name="class_id" required>
            {{#each classes}}
            <option value="{{this.class_id}}" {{#if (eq this.class_id ../studentInClass.class_id)}}selected{{/if}}>
                {{this.class_name}}
            </option>
            {{/each}}
        </select>
        
        <button type="submit">Update</button>
    </form>
</body>
</html>

